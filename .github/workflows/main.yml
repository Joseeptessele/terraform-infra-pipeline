name: "PROD DEPLOY"

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  terraform:
    uses: ./.github/workflows/terraform.yml
    with:
      environment: prod
      aws-assume-role-arn: "arn:aws:iam::070814786851:role/github-actions-Joseeptessele-pipeline"
      aws-region: "sa-east-1"
      aws-statefile-s3-bucket: "joseeptessele-terraform-statefile"
      aws-lock-dynamodb-table: "joseeptessele-terraform-lock"